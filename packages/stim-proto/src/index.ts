/**
 * Shared protocol-contract package for `stim.io` participants.
 *
 * The cold-start baseline intentionally keeps this package minimal.
 * Real shared contract types land in the next execution step.
 */
export const STIM_PROTO_VERSION = "0.1.0-dev.0" as const;
